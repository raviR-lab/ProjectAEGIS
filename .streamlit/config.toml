[server]
headless = true

[browser]
gatherUsageStats = false

# Do not lock base = "dark" — users can pick Light / Dark / System in Settings.
# Custom dashboard CSS follows st.context.theme.type on each rerun.
